{
  "name": "arap-shape-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd .. && bash -lc '. \"$HOME/.cargo/env\" && cargo build -p arap2d --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-dir web/pkg target/wasm32-unknown-unknown/release/arap2d.wasm'",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "delaunator": "^5.0.1",
    "lucide": "^0.555.0"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
